<h1>Книги</h1>

<% if user_signed_in? %>
  <div class="buffer-bottom">
    <%= link_to 'Выложить книгу', new_book_path, class: 'btn btn-success' %>
  </div>
<% end %>

<div class="row buffer-bottom">
  <!-- Рендерю массив всех книг из паршела books/book -->
  <%= render partial: 'books/book', collection: @books %>
</div>
