<%= form_for [@book, rate] do |f| %>
    <%= render partial: 'partials/form_errors', locals: {model: rate} %>

    <div class="field">
      <%= f.number_field :rating, min: 1, max: 5, step: 1 %>
      <%= f.submit 'Оценить', class: 'btn btn-success' %>
    </div>
<% end %>